export default {
    // main function is to position the cursor in correct way.
    watch: {
        textAreaValue() {
            // position cursor
            console.log('positioning cursor')      
        }
    },
}